version: "3.8"

services:
  kata_sandbox_typescript:
    build: .
    container_name: "kata_sandbox_typescript"
    working_dir: "/usr/src/app"
    volumes:
      - ./:/usr/src/app:rw,Z
    tty: true
